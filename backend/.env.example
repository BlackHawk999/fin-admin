# Database (for Render: use DATABASE_URL from dashboard)
DATABASE_URL=postgresql://user:password@localhost:5432/finadmin

# JWT secret (set a strong value in production)
SECRET_KEY=your-secret-key-change-in-production

# CORS origins (comma-separated or leave default)
# CORS_ORIGINS=http://localhost:5173,https://your-frontend.onrender.com

# Optional: first admin user (run: python -m scripts.init_db)
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=admin
